# Umami Analytics - Self-hosted, GDPR-compliant web analytics
# Usage: docker compose -f docker-compose.analytics.yml up -d
# Access: http://localhost:3002 (default admin: umami / umami)

services:
  umami:
    image: ghcr.io/umami-software/umami:postgresql-latest
    ports:
      - "3003:3000"
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@host.docker.internal:5432/umami?sslmode=disable
      - DATABASE_TYPE=postgresql
      - APP_SECRET=${UMAMI_APP_SECRET:-change-me-in-production}
    volumes:
      - umami_data:/app/data
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O /dev/null http://localhost:3000/api/heartbeat || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  umami_data:
