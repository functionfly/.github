{
  "author": "functionfly",
  "name": "json-minify",
  "version": "1.0.0",
  "trust_level": "high",
  "description": "Remove whitespace from JSON to produce a compact representation.",
  "tags": ["json", "minify", "compress", "data-formatting"],
  "visibility": "public",
  "pricing": { "type": "free" },
  "source_hash": "203948f53548cc2f6821743ef892c8298f8fdfc8cb0e80e389ef3d81e207839c",
  "manifest": {
    "name": "json-minify",
    "version": "1.0.0",
    "runtime": "python3.11",
    "title": "JSON Minify",
    "description": "Remove whitespace from JSON to produce a compact representation.",
    "handler": "handler",
    "timeout_ms": 5000,
    "memory_mb": 128,
    "deterministic": true,
    "cache_ttl": 86400,
    "side_effects": "none",
    "idempotent": true,
    "public": true,
    "category": "data-formatting",
    "tags": ["json", "minify", "compress", "data-formatting"],
    "capabilities": [],
    "input": {
      "type": "object",
      "properties": {
        "json": { "type": "string", "description": "JSON string to minify" }
      },
      "example": { "json": "{ \"key\": \"value\" }" }
    },
    "output": {
      "type": "object",
      "properties": {
        "ok": { "type": "boolean" },
        "result": { "type": "string", "description": "Minified JSON string" },
        "error": { "type": "string", "description": "Error message if ok is false" }
      },
      "example": { "ok": true, "result": "{\"key\":\"value\"}" }
    }
  },
  "source": {
    "code": "import json\n\n\ndef handler(event):\n    try:\n        if isinstance(event, dict):\n            data = event.get(\"json\", \"\")\n        else:\n            data = str(event) if event is not None else \"\"\n\n        if data is None or (isinstance(data, str) and not data.strip()):\n            return {\"ok\": False, \"error\": \"Missing required field: json\"}\n\n        if isinstance(data, str):\n            obj = json.loads(data)\n        else:\n            obj = data\n\n        result = json.dumps(obj, separators=(\",\", \":\"), ensure_ascii=False)\n        return {\"ok\": True, \"result\": result}\n    except json.JSONDecodeError as e:\n        return {\"ok\": False, \"error\": f\"Invalid JSON: {str(e)}\"}\n    except Exception as e:\n        return {\"ok\": False, \"error\": str(e)}\n",
    "runtime": "python3.11"
  }
}
