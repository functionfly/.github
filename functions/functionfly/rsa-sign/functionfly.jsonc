{
  "author": "functionfly",
  "name": "rsa-sign",
  "version": "1.0.0",
  "runtime": "python3.12",
  "title": "RSA Sign",
  "description": "Sign a message with an RSA private key using PKCS#1 v1.5 padding.",
  "category": "crypto",
  "tags": ["crypto", "rsa", "sign", "signature", "asymmetric"],
  "input": {
    "type": "object",
    "properties": {
      "message": { "description": "Message to sign" },
      "private_key": { "type": "string", "description": "PEM-encoded RSA private key" },
      "algorithm": { "type": "string", "description": "Hash algorithm", "default": "sha256", "enum": ["sha1", "sha256", "sha384", "sha512"] }
    },
    "required": ["message", "private_key"]
  },
  "output": {
    "type": "object",
    "properties": {
      "ok": { "type": "boolean" },
      "result": { "type": "string", "description": "Base64-encoded signature" },
      "algorithm": { "type": "string" },
      "error": { "type": "string" }
    }
  },
  "deterministic": false,
  "idempotent": false,
  "cache_ttl": 0,
  "timeout_ms": 1000,
  "memory_mb": 128,
  "price_per_call": 0.002,
  "example": {
    "input": { "message": "Hello", "private_key": "-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----" },
    "output": { "ok": true, "algorithm": "sha256" }
  }
}
