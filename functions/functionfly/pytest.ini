[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    e2e: end-to-end tests (run with -m e2e or exclude with -m "not e2e")
addopts = -v --tb=short
filterwarnings =
    ignore::DeprecationWarning
