{
  "author": "functionfly",
  "name": "heap-max",
  "version": "1.0.0",
  "runtime": "python3.12",
  "title": "Max Heap",
  "description": "Create and manipulate a max-heap data structure.",
  "category": "arrays",
  "tags": ["heap", "max-heap", "data-structure", "priority-queue"],
  "input": {
    "type": "object",
    "properties": {
      "items": { "type": "array", "description": "Initial items for the max heap", "default": [] },
      "operations": { "type": "array", "description": "Operations to perform", "items": {
        "type": "object",
        "properties": {
          "action": { "type": "string", "enum": ["push", "pop", "peek", "size", "is_empty"] },
          "value": { "description": "Value to push (required for push action)" }
        },
        "required": ["action"]
      }}
    }
  },
  "output": {
    "type": "object",
    "properties": {
      "ok": { "type": "boolean" },
      "result": {
        "type": "object",
        "properties": {
          "final_heap": { "type": "array", "description": "Final heap as sorted array (largest to smallest)" },
          "operations": { "type": "array" }
        }
      },
      "error": { "type": "string" }
    }
  },
  "deterministic": true,
  "idempotent": false,
  "cache_ttl": 0,
  "timeout_ms": 1000,
  "memory_mb": 128,
  "example": {
    "input": {
      "items": [1, 3, 2],
      "operations": [
        {"action": "push", "value": 5},
        {"action": "pop"},
        {"action": "peek"}
      ]
    },
    "output": {
      "ok": true,
      "result": {
        "final_heap": [1, 2, 3, 5],
        "operations": [
          {"action": "push", "value": 5, "success": true},
          {"action": "pop", "result": 5, "success": true},
          {"action": "peek", "result": 3}
        ]
      }
    }
  }
}