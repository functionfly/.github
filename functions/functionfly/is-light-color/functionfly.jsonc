{
  "author": "functionfly",
  "name": "is-light-color",
  "version": "1.0.0",
  "runtime": "python3.12",
  "title": "Is Light Color?",
  "description": "Check if a color is light (above a luminance threshold). Useful for choosing black or white text on a background.",
  "category": "color",
  "tags": ["color", "light", "luminance", "wcag", "accessibility"],
  "input": {
    "type": "object",
    "properties": {
      "color": { "type": "string" },
      "threshold": { "type": "number", "description": "Luminance threshold (0-1)", "default": 0.5 }
    },
    "required": ["color"]
  },
  "output": { "type": "object", "properties": { "ok": {}, "result": { "type": "boolean" }, "luminance": {}, "error": {} } },
  "deterministic": true, "idempotent": true,
  "cache_ttl": 86400, "timeout_ms": 100, "memory_mb": 64,
  "example": { "input": { "color": "#ffffff" }, "output": { "ok": true, "result": true, "luminance": 1.0 } }
}
