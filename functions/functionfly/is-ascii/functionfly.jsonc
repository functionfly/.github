{
  "author": "functionfly",
  "name": "is-ascii",
  "version": "1.0.0",
  "runtime": "python3.12",
  "title": "Is ASCII",
  "description": "Check whether a string contains only ASCII characters (codepoints 0–127).",
  "category": "validation",
  "tags": ["validation", "ascii", "encoding", "check"],
  "input": {
    "type": "object",
    "properties": {
      "value": { "type": "string", "description": "String to check" }
    },
    "required": ["value"]
  },
  "output": {
    "type": "object",
    "properties": {
      "ok": { "type": "boolean" },
      "value": { "type": "string" },
      "result": { "type": "boolean" },
      "error": { "type": "string" }
    }
  },
  "deterministic": true,
  "idempotent": true,
  "cache_ttl": 3600,
  "timeout_ms": 100,
  "memory_mb": 64,
  "example": {
    "input": { "value": "Hello World" },
    "output": { "ok": true, "value": "Hello World", "result": true }
  }
}
