{
  "author": "functionfly",
  "name": "text-truncate",
  "version": "1.0.0",
  "trust_level": "high",
  "description": "Truncate text to a maximum length with an optional suffix.",
  "tags": ["text", "truncate", "string", "formatting"],
  "visibility": "public",
  "pricing": { "type": "free" },
  "source_hash": "e18f4f352fd2fc415848f0191b671578d37738e3d76f5f22e137c41b2c2f5b92",
  "manifest": {
    "name": "text-truncate",
    "version": "1.0.0",
    "runtime": "python3.11",
    "title": "Text Truncate",
    "description": "Truncate text to a maximum length with an optional suffix.",
    "handler": "handler",
    "timeout_ms": 5000,
    "memory_mb": 128,
    "deterministic": true,
    "cache_ttl": 86400,
    "side_effects": "none",
    "idempotent": true,
    "public": true,
    "category": "text",
    "tags": ["text", "truncate", "string", "formatting"],
    "capabilities": [],
    "input": {
      "type": "object",
      "properties": {
        "text": { "type": "string", "description": "Text to truncate" },
        "max_length": { "type": "integer", "description": "Maximum length of the result including suffix (default: 100)" },
        "suffix": { "type": "string", "description": "Suffix to append when truncated (default: ...)" }
      },
      "example": { "text": "long string", "max_length": 10, "suffix": "..." }
    },
    "output": {
      "type": "object",
      "properties": {
        "ok": { "type": "boolean" },
        "result": { "type": "string", "description": "Truncated string" },
        "error": { "type": "string", "description": "Error message if ok is false" }
      },
      "example": { "ok": true, "result": "long st..." }
    }
  },
  "source": {
    "code": "def handler(event):\n    try:\n        if isinstance(event, dict):\n            text = event.get(\"text\", \"\")\n            max_length = event.get(\"max_length\", 100)\n            suffix = event.get(\"suffix\", \"...\")\n        else:\n            text = str(event) if event is not None else \"\"\n            max_length = 100\n            suffix = \"...\"\n\n        if text is None:\n            return {\"ok\": False, \"error\": \"Missing required field: text\"}\n\n        if not isinstance(text, str):\n            text = str(text)\n\n        try:\n            max_length = int(max_length)\n            if max_length < 0:\n                max_length = 0\n        except (TypeError, ValueError):\n            return {\"ok\": False, \"error\": \"max_length must be an integer\"}\n\n        if not isinstance(suffix, str):\n            suffix = str(suffix)\n\n        if len(text) <= max_length:\n            result = text\n        else:\n            truncate_at = max_length - len(suffix)\n            if truncate_at < 0:\n                truncate_at = 0\n            result = text[:truncate_at] + suffix\n\n        return {\"ok\": True, \"result\": result}\n    except Exception as e:\n        return {\"ok\": False, \"error\": str(e)}\n",
    "runtime": "python3.11"
  }
}
