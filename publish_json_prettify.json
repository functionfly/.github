{
  "author": "functionfly",
  "name": "json-prettify",
  "version": "1.0.0",
  "trust_level": "high",
  "description": "Pretty-print JSON with indentation.",
  "tags": ["json", "format", "prettify", "data-formatting"],
  "visibility": "public",
  "pricing": { "type": "free" },
  "source_hash": "601296ddff23dd61e5fc37b79f77dcbb3e422a2672515cd17d88f84d5080a4ac",
  "manifest": {
    "name": "json-prettify",
    "version": "1.0.0",
    "runtime": "python3.11",
    "title": "JSON Prettify",
    "description": "Pretty-print JSON with indentation.",
    "handler": "handler",
    "timeout_ms": 5000,
    "memory_mb": 128,
    "deterministic": true,
    "cache_ttl": 86400,
    "side_effects": "none",
    "idempotent": true,
    "public": true,
    "category": "data-formatting",
    "tags": ["json", "format", "prettify", "data-formatting"],
    "capabilities": [],
    "input": {
      "type": "object",
      "properties": {
        "json": { "type": "string", "description": "JSON string to prettify" },
        "indent": { "type": "integer", "description": "Number of spaces for indentation (default: 2)" }
      },
      "example": { "json": "{\"key\":\"value\"}", "indent": 2 }
    },
    "output": {
      "type": "object",
      "properties": {
        "ok": { "type": "boolean" },
        "json": { "type": "string", "description": "Pretty-printed JSON string" },
        "error": { "type": "string", "description": "Error message if ok is false" }
      },
      "example": { "ok": true, "json": "{\n  \"key\": \"value\"\n}" }
    }
  },
  "source": {
    "code": "import json\n\ndef handler(event):\n    if isinstance(event, dict):\n        data = event.get(\"data\", event.get(\"json\", event.get(\"text\", event)))\n        indent = event.get(\"indent\", 2)\n    else:\n        data = event\n        indent = 2\n\n    if data is None:\n        return {\"ok\": False, \"error\": \"Input 'data' is required\"}\n\n    try:\n        if isinstance(data, str):\n            obj = json.loads(data)\n        else:\n            obj = data\n        try:\n            indent = int(indent)\n            if indent < 0:\n                indent = 0\n        except (TypeError, ValueError):\n            indent = 2\n        out = json.dumps(obj, indent=indent, ensure_ascii=False)\n        return {\"ok\": True, \"json\": out}\n    except json.JSONDecodeError as e:\n        return {\"ok\": False, \"error\": f\"Invalid JSON: {e!s}\"}\n    except Exception as e:\n        return {\"ok\": False, \"error\": str(e)}\n",
    "runtime": "python3.11"
  }
}
