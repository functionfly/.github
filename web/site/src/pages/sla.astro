---
import Layout from '../layouts/Layout.astro';
import SLAPage from '../components/SLAPage';

const title = 'Service Level Agreement (SLA) — FunctionFly';
const description = 'Uptime commitments, measurement, exclusions, and service credits for FunctionFly Professional and Enterprise platform plans.';
---

<Layout title={title} description={description}>
  <SLAPage client:load />
</Layout>
