---
// Redirect all blog traffic to the new standalone blog at blog.functionfly.com
const blogUrl = import.meta.env.PUBLIC_BLOG_URL || 'https://blog.functionfly.com';

return Astro.redirect(blogUrl);
---
