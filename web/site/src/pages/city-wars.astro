---
import Layout from '../layouts/Layout.astro';
import CityWarsPage from '../components/CityWarsPage';
import { SITE_ORIGIN } from '../config';
import '../styles/sc-main.css';

const title = 'City Wars — Quarterly Bracket Challenge | FunctionFly';
const description = 'Watch the top 8 AI hub cities compete in a single-elimination bracket. City Wars runs quarterly.';
---

<Layout
  title={title}
  description={description}
>
  <CityWarsPage client:load />
</Layout>
