// Tauri v2 preload - contextBridge is injected by Tauri
// This file is loaded via the preload option in tauri.conf.json

export function setupPreload() {
  // Tauri v2 handles context bridge automatically
  // window.electronAPI will be available after Tauri initializes
}