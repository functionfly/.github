/**
 * DEPRECATED: This file is kept for reference but not used.
 * Tailwind CSS v4 uses CSS-first configuration in src/styles/index.css.
 * The @tailwindcss/vite plugin reads from the CSS file directly.
 *
 * To migrate settings from here to v4 format:
 * - Colors/Theme: Use @theme block in CSS
 * - Animations: Use @theme or @keyframes in CSS
 * - Content paths: Use @source in CSS
 */

/** @type {import('tailwindcss').Config} */
export default {
  // Not used - see src/styles/index.css for v4 configuration
  content: [],
  theme: {},
  plugins: [],
};
