{
  "author": "functionfly",
  "name": "base64-decode",
  "version": "1.0.0",
  "trust_level": "high",
  "description": "Decode a Base64 string to plain text. Optional URL-safe input.",
  "tags": ["base64", "decode", "encoding", "text"],
  "visibility": "public",
  "pricing": { "type": "free" },
  "source_hash": "3e85e06bbe78afe0d614a0b55d31cda2937411606531951ae1ca5e555ea14b46",
  "manifest": {
    "name": "base64-decode",
    "version": "1.0.0",
    "runtime": "python3.11",
    "title": "Base64 Decode",
    "description": "Decode a Base64 string to plain text. Optional URL-safe input.",
    "handler": "handler",
    "timeout_ms": 5000,
    "memory_mb": 128,
    "deterministic": true,
    "cache_ttl": 86400,
    "side_effects": "none",
    "idempotent": true,
    "public": true,
    "category": "encoding",
    "tags": ["base64", "decode", "encoding", "text"],
    "capabilities": [],
    "input": {
      "type": "object",
      "properties": {
        "encoded": { "type": "string", "description": "Base64-encoded string to decode" },
        "url_safe": { "type": "boolean", "description": "Use URL-safe Base64 alphabet (default: false)" }
      },
      "example": { "encoded": "SGVsbG8gV29ybGQ=", "url_safe": false }
    },
    "output": {
      "type": "object",
      "properties": {
        "ok": { "type": "boolean" },
        "text": { "type": "string", "description": "Decoded plain text" },
        "error": { "type": "string", "description": "Error message if ok is false" }
      },
      "example": { "ok": true, "text": "Hello World" }
    }
  },
  "source": {
    "code": "import base64\n\ndef handler(event):\n    if isinstance(event, dict):\n        encoded = event.get(\"encoded\", event.get(\"data\", \"\"))\n        url_safe = event.get(\"url_safe\", False)\n    else:\n        encoded = str(event) if event is not None else \"\"\n        url_safe = False\n\n    if not encoded or not str(encoded).strip():\n        return {\"ok\": False, \"error\": \"Input 'encoded' is required and cannot be empty\"}\n\n    try:\n        raw = encoded if isinstance(encoded, bytes) else encoded.encode(\"ascii\")\n        if url_safe:\n            pad = 4 - (len(raw) % 4)\n            if pad != 4:\n                raw += b\"=\" * pad\n            data = base64.urlsafe_b64decode(raw)\n        else:\n            data = base64.b64decode(raw)\n        text = data.decode(\"utf-8\")\n        return {\"ok\": True, \"text\": text}\n    except Exception as e:\n        return {\"ok\": False, \"error\": str(e)}\n",
    "runtime": "python3.11"
  }
}
